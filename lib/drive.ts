import {google} from "googleapis";
export function driveClient(){const email=process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL;const key=process.env.GOOGLE_PRIVATE_KEY?.replace(/\\n/g,"\n");if(!email||!key) throw new Error("Google Drive credentials not configured");const auth=new google.auth.JWT({email,key,scopes:["https://www.googleapis.com/auth/drive"]});return google.drive({version:"v3",auth})}
export async function listDriveTree(){const drive=driveClient();const rootId=process.env.GOOGLE_DRIVE_ROOT_ID;if(!rootId) throw new Error("GOOGLE_DRIVE_ROOT_ID not configured");const out:any[]=[];let pageToken:string|undefined;do{const r=await drive.files.list({q:"'"+rootId+"' in parents and trashed=false",fields:"nextPageToken,files(id,name,mimeType,parents,modifiedTime,webViewLink)",pageToken});out.push(...(r.data.files||[]));pageToken=r.data.nextPageToken||undefined}while(pageToken);return out}